{
  "name": "clients",
  "version": "1.0.0",
  "main": "index.js",
  "author": "Tentáculo.Digital (contato@tentaculo.digital)",
  "scripts": {
    "offline-start": "serverless offline start",
    "service-pack": "export SLS_DEBUG=* && serverless package --verbose",
    "deploy": "export SLS_DEBUG=* && serverless deploy --package .serverless"
  },
  "license": "MIT"
}
